[Unit]
Description=SquishBox UART MIDI bridge

[Service]
Environment=HOME=/root
WorkingDirectory=/root
ExecStart=/usr/bin/ttymidi -s /dev/serial0 -b 38400 -n 'SquishBox minijacks'
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
