# v3 hardware overlay
#  purple PCB with 1 through-hole resistor
lcd_regsel: 4
lcd_enable: 27
lcd_data: [9, 11, 5, 6]
controls:
  button_right:
    type: button
    pin: 3
    actions: {tap: inc, hold: select}
  button_left:
    type: button
    pin: 2
    actions: {tap: dec, hold: back}

